import { Repository } from 'typeorm';
import { IPage, IPagination } from '../shared/interfaces/page.interface';
import { User } from '../users/entities/user.entity';
import { Subcategory } from '../subcategory/entities/subcategory.entity';
import { Section } from '../section/entities/section.entity';
import { Subsection } from '../subsection/entities/subsection.entity';
import { Question } from '../question/entities/question.entity';
import { Application } from '../application/entities/application.entity';
import { Answer } from '../application/entities/answer.entity';
import { Certificate } from '../certificate/entities/certificate.entity';
import { Notification } from '../notification/entities/notification.entity';
import { CategoryFilterOptionsDto } from './dto/category-filter-options.dto';
import { CreateCategoryDto } from './dto/create-category.dto';
import { UpdateCategoryDto } from './dto/update-category.dto';
import { Category } from './entities/category.entity';
export declare class CategoryService {
    private readonly categoryRepo;
    private readonly subcategoryRepo;
    private readonly sectionRepo;
    private readonly subsectionRepo;
    private readonly questionRepo;
    private readonly applicationRepo;
    private readonly answerRepo;
    private readonly certificateRepo;
    private readonly notificationRepo;
    constructor(categoryRepo: Repository<Category>, subcategoryRepo: Repository<Subcategory>, sectionRepo: Repository<Section>, subsectionRepo: Repository<Subsection>, questionRepo: Repository<Question>, applicationRepo: Repository<Application>, answerRepo: Repository<Answer>, certificateRepo: Repository<Certificate>, notificationRepo: Repository<Notification>);
    create(createCategoryDto: CreateCategoryDto): Promise<Category>;
    findAll(options: IPagination, { sort, search, ...filterOptions }: CategoryFilterOptionsDto, user: User): Promise<IPage<Category>>;
    findOne(id: number): Promise<Category>;
    update(id: number, updateCategoryDto: UpdateCategoryDto): Promise<Category>;
    remove(id: number): Promise<void>;
    toggleActive(id: number): Promise<Category>;
}
